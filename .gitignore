# ==========================================================
# ОСНОВНЕ ДЛЯ PYTHON / VIRTUAL ENVIRONMENTS
# ==========================================================

# Віртуальні середовища (venv, virtualenv, Poetry)
# Ігнорувати папку віртуального середовища, де зберігаються залежності
venv/
.venv/
env/
.env/
.python-version
__pycache__/

# Згенеровані байт-код файли
*.pyc

# IDE/Редактори (загальні)
.vscode/          # Visual Studio Code
.idea/            # JetBrains PyCharm/IntelliJ
*.swp             # Vim swap files

# ==========================================================
# DJANGO / DJANGO REST FRAMEWORK (DRF)
# ==========================================================

# Згенеровані файли міграцій
# Залишаємо лише початкові файли міграцій (0001_initial.py)
# Ігноруємо всі файли міграцій, КРІМ __init__.py
*/migrations/*.py
!*/migrations/__init__.py

# Локальні бази даних SQLite
db.sqlite3
*.db

# Файли медіа та статичних файлів, завантажених користувачами
# Залишаємо лише пусті папки для структури (якщо вони потрібні)
/media
/static_collected/
/staticfiles/
/temp/

# Файли, що генеруються під час тестування (якщо використовуються)
.pytest_cache/
htmlcov/
.coverage

# ==========================================================
# ЛОКАЛЬНІ КОНФІГУРАЦІЙНІ ФАЙЛИ
# (СЕКРЕТИ / SECRETS)
# ==========================================================

# Файли налаштувань, що містять SECRET_KEY, паролі до БД (PostgreSQL) та інше
# Переконайтеся, що ви використовуєте environment variables або окремий файл для секретів
local_settings.py
settings/local.py
config/settings/local.py
.env
.local
*.log

# ==========================================================
# ФРОНТЕНД / NODE / JS
# (Якщо використовуєте npm/yarn для фронтенду, що часто буває)
# ==========================================================

# Модулі Node (якщо використовується для збирання JS)
/node_modules/

# Згенеровані файли TypeScript (якщо є)
*.tsbuildinfo